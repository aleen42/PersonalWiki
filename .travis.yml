language: generic

before_deploy: "echo (before_deploy)test;"
deploy:
        provider: pages
        skip_cleanup: true
        github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
        on:
                branch: master
        local_dir: ./gh-pages/
